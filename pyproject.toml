[project]
name = "greek-study-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=3.4.0",
]

[dependency-groups]
dev = [
    "textual-dev>=1.7.0",
]
